<?xml version="1.0" encoding="utf-8"?>
<!-- Hackathon: plain HTTP to the server on the LAN. Every device request is signed (contract §2.3). -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
